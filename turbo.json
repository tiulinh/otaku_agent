{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "persistent": true,
      "cache": false
    },
    "build:backend": {
      "outputs": ["dist/**"]
    },
    "build:frontend": {
      "outputs": ["dist/frontend/**"]
    }
  }
}

